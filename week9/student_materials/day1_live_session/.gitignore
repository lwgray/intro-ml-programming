# Image datasets — regenerate from the notebooks' download/build cells; don't commit
flowers/
flowers_hard/
cats_and_dogs_filtered/
eurosat/
*.zip
# Saved models — except the shipped fallback checkpoint (README references it)
*.keras
!week9_flowers_finetuned.keras
